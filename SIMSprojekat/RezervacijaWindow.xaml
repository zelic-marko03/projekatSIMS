<Window x:Class="SIMSprojekat.RezervacijaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nova rezervacija" Height="280" Width="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Ime apartmana -->
        <TextBlock Text="Ime apartmana:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="ApartmanBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Datum od -->
        <TextBlock Text="Datum od:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <DatePicker x:Name="DatumOdPicker" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Datum do -->
        <TextBlock Text="Datum do:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <DatePicker x:Name="DatumDoPicker" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Dugme -->
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Rezerviši" Width="120" Height="32" 
                    Click="Rezervisi_Click"/>
        </StackPanel>
    </Grid>
</Window>