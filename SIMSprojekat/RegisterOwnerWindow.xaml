<Window x:Class="SIMSprojekat.RegisterOwnerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registracija vlasnika" Height="420" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <!-- definicija redova -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Ime -->
        <TextBlock Text="Ime:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="ImeBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Prezime -->
        <TextBlock Text="Prezime:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="PrezimeBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- JMBG -->
        <TextBlock Text="JMBG:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="JmbgBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Email -->
        <TextBlock Text="Email:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="EmailBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Telefon -->
        <TextBlock Text="Telefon:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="TelefonBox" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Lozinka -->
        <TextBlock Text="Lozinka:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <PasswordBox x:Name="PassBox" Grid.Row="5" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Dugmad -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Otkaži" Width="110" Margin="0,0,8,0" Click="Cancel_Click" Height="44"/>
            <Button Content="Registruj vlasnika" Width="160" Click="Save_Click" Height="42"/>
        </StackPanel>
    </Grid>
</Window>
